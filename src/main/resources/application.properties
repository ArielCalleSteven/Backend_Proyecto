# ==========================================
# 🏠 MODO CASA (TU PC) - AHORA ESTÁ ENCENDIDO ✅
# ==========================================
# Usa esto mientras pruebas en tu compu para que no te de error
spring.datasource.url=jdbc:mysql://localhost:3306/portafolio_db
spring.datasource.username=root
# CAMBIA 'admin' POR TU CONTRASEÑA REAL DE MYSQL SI ES OTRA
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==========================================
# ☁️ MODO NUBE (RAILWAY) - AHORA ESTÁ APAGADO ❌
# ==========================================
# (Solo le puse un # al principio para apagarlo temporalmente)
# Cuando vayamos a subir a GitHub, le quitamos el # a estas líneas y se lo ponemos a las de arriba
# spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}
# spring.datasource.username=${MYSQLUSER}
# spring.datasource.password=${MYSQLPASSWORD}

# ==========================================
# ⚙️ CONFIGURACIÓN GENERAL (SIRVE PARA LOS DOS)
# ==========================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
server.port=${PORT:8080}

# ==========================================
# 📧 CORREOS (ESTO ES LO NUEVO QUE QUEREMOS PROBAR)
# ==========================================
# CONFIGURACIÓN GMAIL SMTPS (PROTOCOLO SEGURO)
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=a.calleduma123@gmail.com
spring.mail.password=vldd wpnz axrn mutt

# 🔥 El truco: Usar el protocolo SMTPS explícitamente
spring.mail.protocol=smtps

# Propiedades específicas para SMTPS
spring.mail.properties.mail.smtps.auth=true
spring.mail.properties.mail.smtps.starttls.enable=false
spring.mail.properties.mail.smtps.ssl.enable=true
spring.mail.properties.mail.smtps.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtps.timeout=5000
spring.mail.properties.mail.smtps.connectiontimeout=5000

# Aseguramos que el transporte use el protocolo correcto
spring.mail.properties.mail.transport.protocol=smtps
spring.mail.properties.mail.debug=true